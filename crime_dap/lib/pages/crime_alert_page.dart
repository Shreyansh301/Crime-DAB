import 'package:flutter/material.dart';

class CrimeAlertPage extends StatelessWidget {
  const CrimeAlertPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Crime Alert Page',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
